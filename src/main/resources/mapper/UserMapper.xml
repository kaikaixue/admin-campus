<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.campus.admin.campus.mapper.UserMapper">
    <select id="queryUser" resultType="top.campus.admin.campus.domain.Do.UserDo">
        select pk_id,avatar,name,age,department,grade,dormitory,phone,create_time from t_user where is_delete = 0
    </select>
</mapper>